<ul class="list-unstyled">
  <% @items.each do |item| %>
    <li>
      <div class="card-item">
        <div class="card-details">
          <h4><%= item.name %></h4>
          <p><%= item.description %></p>
          <p>QR Code: <%= item.qr_number %></p>
        </div>
        <div class="card-menu dropdown">
          <div class="dropdown">
            <i class="fas fa-ellipsis-v dropdown-toggle" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"></i>
            <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
              <%= link_to "Edit", edit_item_path(item), class: "dropdown-item" %>
              <%= link_to "Delete", disabled_item_path(item), class: "dropdown-item", method: :patch, data: { confirm: "Are you sure you want to delete this item?" } %>
            </div>
          </div>
          <div id="marker-finding">
            <% item.findings.each do |finding| %>
              <% if finding.state == "pending" %>
                <%= link_to finding_path(finding) do %>
                  <i class="fas fa-map-marker-alt"></i>
                <% end %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </li>
  <% end %>
</ul>


